# 📱 프론트엔드 3번 제작 계획서 (구독 핵심 UI)

## 0. 개발 환경 및 기술 스택
| 영역 | 기술 | 비고 |
|------|------|------|
| **언어/프레임워크** | Java 17, Spring Boot 3.x | 백엔드 중심 |

| **빌드 도구** | Gradle (backend/), Node.js + Vite (frontend/) | 환경 분리 |

| **데이터베이스** | MySQL 8.0 | |

| **프론트엔드** | Thymeleaf 3.x, HTML5, CSS3, Vanilla JS | 통합형 방식 |

| **배포/인프라** | GitHub Actions, AWS EC2 | |

## 1. 상세 제작 페이지 및 핵심 요소

### ① 메인 및 플랜 목록 (`templates/plan/list.html`)

    - [ ] **플랜 카드 리스트**: `th:each`를 활용한 전체 구독 서비스 목록 출력

    - [ ] **오늘의 체크인 토글**: 당일 사용 여부를 기록하는 인터랙티브 버튼

    - [ ] **30일 히트맵 그리드**: 30개의 `<div class="heat-*">`를 통한 사용량 시각화

    - [ ] **경보 배지**: `alert_level`에 따른 상태 표시 (NORMAL, WARNING, DANGER, CRITICAL)

### ② 플랜 상세 페이지 (`templates/plan/detail.html`)

    - [ ] **확대 히트맵**: 특정 서비스의 상세 사용 통계 및 
    그리드 확장형

    - [ ] **서비스 상세 정보**: 결제 금액, 주기, 관리 URL 등 상세 스펙 노출

### ③ 구독 신청 폼 (`templates/subscribe/form.html`)
    - [ ] **신청 입력 필드**: 구독 시작일 선택 및 서비스 옵션 지정

    - [ ] **약관 동의**: 필수 약관 동의 체크박스 구성

### ④ 신청 결과 (`templates/subscribe/result.html`)

    - [ ] **상태 메시지**: 구독 신청 성공/실패 여부 시각화

    - [ ] **내비게이션**: 메인 대시보드로 돌아가기 버튼

### ⑤ 내 구독 관리 (`templates/member/subscriptions.html`)

    - [ ] **마이 구독 리스트**: 현재 본인이 구독 중인 서비스 카드 목록

    - [ ] **관리 액션**: 즉시 해지(Quick Cancel) 버튼 및 알림 설정(Toggle) 기능

### ⑥ 대체 추천 (`templates/recommendation.html`)

    - [ ] **추천 카드 UI**: "이 구독 대신 이걸 써보세요" 서비스 비교 및 추천 로직 UI

    - [ ] **비용 절감 표시**: 대체 시 절약 가능한 예상 금액 시각화

### ⑦ 해지 다이어리 (`templates/cancellation-log.html`)

    - [ ] **과거 기록 리스트**: 이전에 해지했던 서비스들의 내역 출력

    - [ ] **감정/사유 태그**: 해지 당시 선택한 이유 및 메모 표시

## 2. 기술적 준수 사항 (프론트 1 협업)

    - **레이아웃 상속**: 반드시 `layout.html`을 `th:replace`하여 사용 (직접 `<head>`, `<body>` 작성 금지)

    - **공통 CSS 사용**: `.btn`, `.card`, `.form-group`, `.badge-*` 등 프론트 1이 정의한 클래스 우선 적용

    - **히트맵 구현**: 
        - `th:each` 문을 사용하여 30개 그리드 동적 생성
        - 백엔드 2번이 전달하는 `usageStats` 데이터 바인딩


## 3. 백엔드 2번과의 협업 규약 (Model Key)
| 화면 | 필요한 데이터 (Model Key) |
|------|------------------------|
| 플랜 목록 | `plans` |
| 플랜 상세 | `plan` |
| 내 구독 | `subscriptions` |
| 사용 통계 | `usageStats` |
| 추천 목록 | `recommendations` |

---

## 4. 현재 진행 상황 및 구축 아키텍처 (Update: 2026-06-04)

### ✅ 완료된 작업 (Milestones)
- [x] **프로젝트 폴더 분리**: `backend/` (Spring Boot)와 `frontend/` (Vite) 분리 완료
- [x] **Vite MPA 환경 구축**: 7개 핵심 페이지를 독립적으로 빌드/테스트할 수 있는 멀티 페이지 설정 완료
- [x] **가이드라인 준수 폴더 재편**: 백엔드 `templates/` 구조와 일치하는 `frontend/pages/` 하위 도메인 폴더(`plan/`, `subscribe/`, `member/`) 구성 완료
- [x] **정적 HTML 뼈대 생성**: 모든 작업 대상 페이지(7종)의 기초 HTML/CSS/JS 뼈대 생성 완료
- [x] **백엔드 통합 빌드 설정**: `npm run build` 시 결과물이 백엔드 `static/`으로 자동 복사되도록 `vite.config.js` 최적화 완료


### 📂 현재 구축된 프론트엔드 구조
```text
frontend/
├── pages/
│   ├── member/
│   │   └── subscriptions.html      // 내 구독 관리
│   ├── plan/
│   │   ├── detail.html             // 플랜 상세 페이지
│   │   └── list.html               // 플랜 목록
│   ├── subscribe/
│   │   ├── form.html               // 구독 신청 폼
│   │   └── result.html             // 신청 결과
│   ├── cancellation-log.html       // 구독 취소
│   └── recommendation.html         // 추천 카드 UI
├── public/
│   ├── favicon.svg
│   └── icons.svg
├── src/
│   ├── assets/
│   ├── css/
│   │   └── style.css
│   ├── js/
│   └── main.js
├── index.html
├── package.json
└── vite.config.js
```

### 🚀 다음 작업 예정
- [ ] 각 페이지별 세부 UI 컴포넌트(히트맵 그리드, 경보 배지 등) 구현
- [ ] Thymeleaf 이관을 고려한 데이터 바인딩용 가짜 데이터(Mock Data) 로직 고도화
